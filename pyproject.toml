[build-system]
requires = ["setuptools", "wheel"] #, "setuptools-rust"]
build-backend = "setuptools.build_meta"

[project]
name = "corral_crowding"
description = ""
version = "0.1.0"
authors = [{ name = "Evan McKinney" }]
requires-python = ">=3.9"
dependencies = [
    "numpy",
    "qiskit",
    "qutip",
    "qutip-qip",
    "scqubits",
    "weylchamber",
    "networkx",
    "tqdm",
    "cplex",
    "docplex",
    "matplotlib",
    "scienceplots",
    "lovelyplots",
    "openfermion>=0.11.0",
    "joblib>=1.5.3",
    "jax>=0.4.30",
    "qutip-qoc>=0.2.0",
    "qutip-jax>=0.0.1",
    # used directly throughout time_dependent_solver (linalg.expm, optimize,
    # sparse); it was arriving only transitively via qutip.
    "scipy",
    # zhou_coupler.use_gpu() imports it; it was arriving only transitively.
    "diffrax>=0.6",
]

[project.optional-dependencies]
# CUDA build of jax for the batched engine (jax_engine.py). Install on the cluster
# AFTER the base env, and match the CUDA module the node actually provides:
#     module load cuda && uv pip install -e '.[gpu]'
# Nothing else changes: the engine is xp-generic, so the same code runs on CPU.
gpu = ["jax[cuda12]>=0.4.30"]
dev = ["ipykernel", "pre-commit", "pylatexenc", "ipywidgets", "ipympl"]
format = [
    "pre-commit",
    "black[jupyter]",
    "isort",
    "tomli",
    "pydocstyle",
    "ruff",
    "docformatter[tomli]",
]
test = ["pytest"]

[tool.ruff]
target-version = "py312"
fix = true
extend-include = ["*.ipynb"]

[tool.ruff.lint]
select = ["D"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.isort]
profile = "black"


# [[tool.setuptools-rust.ext-modules]]
# target = "quantum_logical._lib"
# # ^-- The last part of the target name (e.g. "rust") should match lib.name in Cargo.toml,
# #     but you can add a prefix to nest it inside of a parent Python package or namespace.
# #     Note that lib.name may not be defined in the Cargo.toml, but you still
# #     have to match the name of the function with the `#[pymodule]` attribute.
# path = "rustlib/Cargo.toml"
# # ^-- Default value for cargo's manifest (can be omitted)
# #     Each manifest can have a single [lib] definition.
# #     To specify multiple extension modules you can use different toml files (one each).
# binding = "PyO3" # Default value, can be omitted
# debug = false
# # See reference for RustExtension in https://setuptools-rust.readthedocs.io/en/latest/reference.html
